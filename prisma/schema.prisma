generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// No models yet. This schema establishes the database connection layer only.
// Business domains (auth, users, tenants, products, orders, ...) will add
// their models here in later phases.
